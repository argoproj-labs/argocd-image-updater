apiVersion: apps/v1
kind: Deployment
metadata:
  name: hydrator-helm-app
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: hydrator-helm-app
  template:
    metadata:
      labels:
        app: hydrator-helm-app
    spec:
      containers:
      - name: helm-container
        image: "{{ .Values.image.name }}:{{ .Values.image.tag }}"
