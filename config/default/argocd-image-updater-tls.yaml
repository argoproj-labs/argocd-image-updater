apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: argocd-image-updater-tls
  labels:
    app.kubernetes.io/name: argocd-image-updater-tls
    app.kubernetes.io/part-of: argocd-image-updater-controller
data:
  # Override with your own TLS certificate and key (base64 encoded).
  # If this secret is not provided, a self-signed certificate will be generated.
  tls.crt: ""
  tls.key: ""
