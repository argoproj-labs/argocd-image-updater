apiVersion: v1
kind: Secret
metadata:
  name: argocd-image-updater-secret
  labels:
    app.kubernetes.io/name: argocd-image-updater-secret
    app.kubernetes.io/part-of: argocd-image-updater-controller
# registry-creds: base64 of "login:password" for registries using secret:... in registries.conf.
# Example: echo -n 'e2e:e2e' | base64
data:
  registry-creds: ZTJlOmUyZQ==
