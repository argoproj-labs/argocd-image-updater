run:
  timeout: 2m
linters:
  enable:
    - vet
    - goimports
    - ineffassign
    - unconvert
    - unparam
    - unused
linters-settings:
  goimports:
    local-prefixes: github.com/argoproj-labs/argocd-image-updater
issues:
  exclude-files:
    - ".*\\.pb\\.go"
  exclude-dirs:
    - vendor/
